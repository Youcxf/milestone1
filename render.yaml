services:
  - type: web
    name: milestone1-backend
    env: docker
    plan: free
    autoDeploy: true
    buildCommand: ./gradlew build
    startCommand: java -jar build/libs/milestone1-0.0.1-SNAPSHOT.jar
    envVars:
      - key: JAVA_VERSION
        value: 17
